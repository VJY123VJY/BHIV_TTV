# Core Framework & Server
fastapi>=0.104.0
uvicorn>=0.24.0
pydantic>=2.5.0
pydantic-settings>=2.0.0
python-multipart>=0.0.6
python-dotenv>=1.0.0

# HTTP & Networking
requests>=2.31.0
httpx>=0.25.0

# Computer Vision & Media Processing
numpy>=1.24.0
Pillow>=10.0.0
opencv-python>=4.8.0

# Speech Synthesis (TTS)
edge-tts>=6.1.0
gTTS>=2.4.0

# Public social/media reference retrieval (YouTube, Instagram, Facebook, X)
yt-dlp>=2024.8.0

# AI Provider SDKs (Optional / Pluggable)
openai>=1.10.0
google-generativeai>=0.4.0

# Testing & Quality
pytest>=7.4.0
pytest-asyncio>=0.21.0

# ── Wan 2.2 TI2V-5B Video Generation ─────────────────────────────────────────
# Required only when VIDEO_PROVIDER=wan (cloud NVIDIA GPU deployment).
# NOTE: torch is NOT listed here — install it FIRST with the correct CUDA
#       version for your cloud GPU before running pip install -r requirements.txt
#       See: https://pytorch.org/get-started/locally/
diffusers>=0.33.0
transformers>=4.51.0
accelerate>=1.6.0
safetensors>=0.5.3
imageio>=2.37.0
